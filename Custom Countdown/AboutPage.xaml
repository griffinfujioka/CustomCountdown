<UserControl
    x:Name="pageRoot"
    x:Class="Custom_Countdown.AboutPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Custom_Countdown"
    xmlns:common="using:Custom_Countdown.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid>
        <StackPanel>
            <TextBlock Text="Custom Countdown" FontFamily="Segoe UI" FontWeight="SemiLight" FontSize="26.667" />
            <TextBlock Text="Created by Griffin Fujioka and David Heyduck. Both are students at Washington State University. &#xA;&#xA;Go Cougs!&#xA;" FontFamily="Segoe UI" FontWeight="SemiLight" FontSize="14" TextWrapping="Wrap" />
            
        </StackPanel>
    </Grid>
</UserControl>
